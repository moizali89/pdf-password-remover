[project]
name = "pdf-password-remover"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pikepdf>=9.0.0",
    "flask>=3.0.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
